/* Comentário de bloco correto */
int main() {
    int x = 10;
    float y = 3.14;
    char letra = "a";

    if (x >= 10 && y != 0) {
        println("Valor de x");
    } else {
        println("Outro valor");
    }

    for (int i = 0; i < 5; i = i + 1) {
        x = x + i;
    }

    while (x > 0) {
        x = x - 1;
    }
@@@

    x = x % 2;

    "string nao fechada

    /* comentário não fechado
}